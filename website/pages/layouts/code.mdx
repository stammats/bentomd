import { SlidePreview } from '../../components/SlidePreview'

# code

Full-slide code block with heading context. Use for showing implementation details, API examples, and live code.


## Preview

<SlidePreview code={`---
title: "Code Example"
aspectRatio: "16:9"
palette:
  primary: "#059669"
---

---
layout: default
heading: "Define Once, Validate Everywhere"
summary: "A single Zod schema powers your API types, runtime validation, and client SDK"
footer: true
pageNumber: true
---

\`\`\`typescript
import { z } from 'zod';
import { router, publicProcedure } from './trpc';

const CreateUserSchema = z.object({
  name: z.string().min(2).max(100),
  email: z.string().email(),
  role: z.enum(['admin', 'member', 'viewer']),
});

export const userRouter = router({
  create: publicProcedure
    .input(CreateUserSchema)
    .mutation(async ({ input, ctx }) => {
      const user = await ctx.db.user.create({ data: input });
      return { id: user.id, ...input };
    }),
});
\`\`\``} />

## Example

```yaml
---
layout: default
heading: "Define Once, Validate Everywhere"
summary: "A single Zod schema powers your API types and validation"
---

\`\`\`typescript
import { z } from 'zod';

const Schema = z.object({
  name: z.string().min(2),
  email: z.string().email(),
});
\`\`\`
```

## Tips

- The `default` layout with a fenced code block is the recommended way to show code
- The dedicated `code` layout also exists for structured code display with language badges
- Keep code to ~15 lines to avoid overflow
- Use comments in the code to highlight key insights
- The auto-scaling system will shrink text if code is too long
